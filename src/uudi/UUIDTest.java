/*
 * package uudi;
 * 
 * import java.util.UUID;
 * 
 * public class UUIDTest { public void fun() { UUID uuid = UUID.randomUUID();
 * String string =uuid.toString(); string = string.replace("-", ""); string =
 * string.toUpperCase();
 * 
 * } }
 */